import * as React from 'react'

export interface IInterpreterProps {
}

export function Interpreter({}: IInterpreterProps) {
  return (
    <div id="rs-interpreter" className="rs-interpreter">
      <h4>Interpreter</h4>
    </div>
  )
}
